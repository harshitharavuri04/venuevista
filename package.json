{
  "name": "venue-vista-frontend-only",
  "private": true,
  "engines": {
    "node": "18.17.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "cd user_management_frontend/venue-vista-frontend && npm ci && npm run build",
    "start": "cd user_management_frontend/venue-vista-frontend && npx serve -s build -p $PORT",
    "postinstall": "cd user_management_frontend/venue-vista-frontend && npm install"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}