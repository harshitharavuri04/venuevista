{
  "name": "venue-vista-unified",
  "private": true,
  "engines": {
    "node": "18.17.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "cd user_management_frontend/venue-vista-frontend && npm ci && npm run build",
    "start": "node server.js",
    "postinstall": "npm run install:backends",
    "install:backends": "cd user_management_backend && npm ci && cd ../booking_management_backend && npm ci && cd ../venue_management_backend && npm ci"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  }
}